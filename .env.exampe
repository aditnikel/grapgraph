# HTTP
HTTP_ADDR=:8080

# Graph / Redis
GRAPH_NAME=fraudnet
REDIS_ADDRS=localhost:6379
REDIS_PASSWORD=

# Timeouts / logging
DB_TIMEOUT_MS=1500
LOG_LEVEL=info

# Subgraph defaults (used when request fields are missing/zero)
DEFAULT_MAX_NODES=200
DEFAULT_MAX_EDGES=400
DEFAULT_MIN_EVENT_COUNT=1
DEFAULT_RANK_BY=event_count_30d
